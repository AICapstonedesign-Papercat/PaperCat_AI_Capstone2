# 실제 키는 .env 에 쓴다(.gitignore로 제외됨). 이 파일은 형식만 보여주는 템플릿.

# OpenAI 호환 엔드포인트면 어디든 됨 — 공급자 교체 시 이 두 줄만 바꾸면 된다.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1

# 임베딩 모델. 기획서가 1536차원 기준으로 용량 산정을 해둬서 기본값을 여기에 맞춤.
EMBED_MODEL=text-embedding-3-small
